interface DeviceWidthInterface {
  minDesktop: Number;
  maxMobile: Number;
  maxTablet: Number;
}

const deviceWidth: DeviceWidthInterface = {
  minDesktop: 1024,
  maxMobile: 768,
  maxTablet: 1024,
};

export default deviceWidth;
